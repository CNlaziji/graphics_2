负责角色
核心研发内容（纯代码任务）
交付产物与标准接口
模块对接与协同机制
组长（成员A）
（核心渲染管线与阴影模块）
1. 搭建基础 CMake 编译环境，初始化 GLFW 窗口与 GLAD 上下文。
2. 封装帧缓冲（FBO），实现 Shadow Map 深度贴图的创建与生成通路（Shadow Pass）。
3. 编写核心渲染循环，控制双通路渲染管线（先渲染深度，再渲染主场景 Lighting Pass）。
4. 统一管理多光源（方向光+点光源衰减+聚光灯）的 Uniform 传递。
WindowManager.hpp
ShadowPipeline.hpp

主程序骨架：
main.cpp（包含主渲染循环通路）
核心枢纽：接收成员D的相机矩阵、成员E的UI参数以及成员B/C的模型和纹理，在主渲染循环中进行全局管线流转与 Shader 变量绑定。
成员 B
（模型与网格模块）
1. 引入 Assimp 库，编写通用的 Mesh（网格）类和 Model（模型）加载类。
2. 实现解析通用 3D 模型文件（如 .obj / .gltf）的递归节点逻辑。
3. 负责在显存中生成并管理顶点数组对象（VAO）、顶点缓冲对象（VBO）和索引缓冲对象（EBO）。
Model.hpp
Mesh.hpp

标准公共接口：
void Model::Draw(Shader &shader)
对纹理模块：加载网格数据时，需调用成员C的纹理生成与绑定逻辑。
对主程序：类结构完成后集成至主干，由核心渲染循环在主渲染通路内统一调用 Draw 方法。
成员 C
（纹理与材质模块）
1. 引入 stb_image.h 库，编写纹理加载通用函数，读取图片（PNG/JPG）并生成 OpenGL 纹理对象。
2. 配置纹理过滤与环绕参数（如 GL_REPEAT、GL_LINEAR_MIPMAP_LINEAR 并生成 Mipmaps）。
3. 在代码层设计材质结构体（包含 DiffuseMap 漫反射、SpecularMap 镜面高光贴图通道），实现不同模型（石料、金属、木头等）的质感差异。
Texture.hpp
（或集成于 Mesh 内部的纹理管理逻辑）

标准公共接口：
unsigned int LoadTexture(const char* path)
对模型模块：配合成员B，确保模型在解析完 Mesh 拓扑结构后，能正确绑定并激活对应的纹理单元（如 GL_TEXTURE0 等）。
成员 D
（相机与动态模块）
1. 编写 FPS 漫游相机类（Camera.hpp），处理键盘 W/A/S/D 平面移动和鼠标位置回调（通过偏航角与俯仰角实时更新相机方向向量）。
2. 计算并输出相机的观察矩阵（View Matrix）和透视投影矩阵（Projection Matrix）。
3. 刚体动画实现：在每帧主循环里，利用时间轴函数产生增量，通过矩阵变换（glm::rotate）实时计算出旋转的模型矩阵，使场景中特定物体（如魔法阵、旋转机关）产生动态效果。
Camera.hpp

标准公共接口：
glm::mat4 Camera::GetViewMatrix()
glm::mat4 Camera::GetProjectionMatrix()
对主程序：将每帧计算更新的 View 矩阵、Projection 矩阵以及相机空间位置（cameraPos）实时传递给主渲染循环，用以更新核心着色器管线的全局 Uniform 变量。
成员 E
（UI 与场景集成模块）
1. 负责在工程中集成 Dear ImGui 库，初始化 UI 窗口上下文。
2. 编写屏幕实时控制面板，将核心着色器中定义的全局光照 Uniform 变量（如环境光强度、灯光颜色、点光源位置等）绑定到 UI 控件上，实现运行期实时调参。
3. 场景拼装集成：搜集并规范至少 5 个 3D 模型资产，在主程序渲染循环中利用矩阵变换（平移、缩放、旋转）对各个资产进行空间布局，拼装出具有封闭空间感的室内/半室内场景。
GuiController.hpp

交付产物：
主循环中具体的模型矩阵空间布局代码段，以及 ImGui 渲染控制函数。
对主程序：调用成员B和成员C封装的模型对象，在核心主循环的 Lighting Pass 通路中通过控制 Model 矩阵进行“积木式”场景搭建；同时通过 UI 交互界面改变核心管线中的光源参数变量。
